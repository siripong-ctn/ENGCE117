#include <stdio.h>

int main() {
    int N, i;
    
    // รับค่า N (จำนวนรอบ)
    if (scanf("%d", &N) != 1) {
        return 1; // Handle input failure
    }
    
    // TODO: Implement the for loop to print the message N times

    return 0;
}